[package]
name = "maa-types"
version = "0.1.0"
edition = "2021"
license.workspace = true
homepage.workspace = true
repository.workspace = true

[features]
serde = ["dep:serde"]
prost = ["dep:prost"]

[dependencies]
prost = { workspace = true, optional = true }
serde = { workspace = true, optional = true }

[dev-dependencies]
# Ideally this should be an optional dependency. However, cargo doesn't support optional dev-dependencies yet.
serde_test = { workspace = true }
